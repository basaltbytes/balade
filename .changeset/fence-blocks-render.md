---
"balade": minor
---

Top-level fenced code blocks now render in the review app as read-only text — highlighted when the language is known, plain otherwise, so pseudo-code explanations reach the reader. ```mermaid fences still render as diagrams. A fence nested in a blockquote or list keeps the warning.
