---
"balade": minor
---

The authoring skeleton's closing Full PR diff section now demonstrates the grouped `{% filegroup /%}` form, so generated walkthroughs group large diffs into thematic sections instead of leaving one flat file list.
