trustPolicy: no-downgrade

# Old releases that predate npm provenance; excluded case by case, never wholesale.
trustPolicyExclude:
  - undici-types@6.21.0
  - semver@6.3.1

onlyBuiltDependencies:
  - esbuild
  # workerd ships the Workers runtime binary; its postinstall places it.
  - workerd
